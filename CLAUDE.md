# Claude 项目入口

Claude Code 每次从本仓库启动时，必须先阅读并完整执行 [项目 Agent 地图](AGENTS.md) 中的“固定启动顺序”。

特别要求：

- 读取 `../JDAgentAgentContext/docs/bootstrap-guide.md` 这一唯一完整协作指南。
- 明确读取 `../JDAgentAgentContext/claude/CLAUDE.md`、`index.md` 和 `current.md`，再按索引读取任务相关文件。
- 不得读取或修改 `../JDAgentAgentContext/codex/`、`../JDAgentAgentContext/cursor/`。
- 产品需求、架构、决策、代码、测试和当前状态始终以本仓库为唯一权威来源。
- 如果同级 Context 仓库不可用，按 `AGENTS.md` 的降级规则继续，并明确报告缺失。
