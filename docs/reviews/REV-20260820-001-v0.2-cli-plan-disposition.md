# REV-20260820-001：v0.2 CLI 计划评审处置

状态：`Approved`

评审方：Cursor

处置方：Codex；用户批准

评审日期：2026-08-20

处置日期：2026-08-20

被评审 revision：`6f34007a4e7bbf66f39c12237ef30c4f51815e5c`

最终结论：`pass_with_findings`

## 范围与证据

原始独立评审位于工作区临时目录
`tmp/reviews/REV-20260820-001-v0.2-cli-implementation.md`，不作为产品仓库依赖。本记录将其
发现、处置和后续闸门纳入权威项目事实。

处置时确认：

- 产品仓库 HEAD 仍为被评审 revision，工作树干净；评审指纹仍适用。
- v0.2 总体目标、范围、ADR-0004 和 CLI 应用层方向不需要推翻。
- 主要缺口是 v0.1 Runtime 契约尚未容纳 v0.2 Recovery/Approval 行为，以及若干接口在实施
  计划中被留到编码期决定。
- 用户确认 Session 级允许规则在 `/exit` 后恢复同一 Session 时继续有效；规则必须可见、
  可撤销，并受用户级权限上限约束。

本轮只修订文档合同与计划，不实现产品代码。

## 发现处置

### REV-20260820-001-F01

```yaml
finding_id: REV-20260820-001-F01
disposition: accepted
rationale: >
  FR-206 的显式最终半行恢复与 v0.1 JSONL reader 的严格 fail-closed 合同确有冲突。
  普通 SessionPort.read 继续严格拒绝损坏；v0.2 通过显式 Recovery 模块在备份、完整前缀
  验证和用户可见诊断后修复最终半行，再交回严格 reader。
resolution_revision: pending
```

处置：修订 Runtime 契约，区分普通读取与显式恢复路径；M3 未通过契约闸门前不得实现修复。

### REV-20260820-001-F02

```yaml
finding_id: REV-20260820-001-F02
disposition: deferred
rationale: >
  Session 名称、workspace 归属、事件类型与旧 Schema 兼容确需在编码前冻结，但不阻塞 M1
  的配置和 Headless 合同。将其设为 M2 开工闸门；未批准事件形状前不得实现 Catalog。
resolution_revision: null
```

触发条件：M2 开工前批准 Session 生命周期事件补充，覆盖默认名称、rename、workspace、
Schema 版本和 v0.1 导入行为。

### REV-20260820-001-F03

```yaml
finding_id: REV-20260820-001-F03
disposition: accepted
rationale: >
  M1 无法在缺少数字退出码、JSON 必填字段、Headless 判定和 v0.1 flag 兼容表时测试先行。
  合同必须在 M1 编码前写入 CLI 架构；M8 只负责真实安装与完整子进程验证，不重新定义合同。
resolution_revision: pending
```

处置：冻结退出码、JSON v1、stdout/stderr、交互/Headless 判定与 v0.1 flag 兼容表。

### REV-20260820-001-F04

```yaml
finding_id: REV-20260820-001-F04
disposition: accepted
rationale: >
  v0.1 ApprovalDecision 无法表达 Session 级窄范围允许。ApprovalPort 应返回结构化
  ApprovalOutcome；规则由 Permission 模块匹配和持久化，Terminal 只收集用户选择。
  用户已确认规则在退出并恢复同一 Session 后继续有效。
resolution_revision: pending
```

处置：冻结 `ApprovalOutcome`、最小权限上限、Session Rule 持久生命周期和规范化目标来源；
具体事件 payload 在 M4 开工闸门完成。

### REV-20260820-001-F05

```yaml
finding_id: REV-20260820-001-F05
disposition: deferred
rationale: >
  异常恢复 Session 的父引用、事件复制或恢复快照属于 M3 的持久化设计，不应在 M1 前仓促
  选择，也不能留到 M3 编码过程中决定。
resolution_revision: null
```

触发条件：M3 开工前批准一页恢复设计记录，固定 provenance、物理×逻辑分类到用户动作的映射、
`TOOL_EXECUTION_STARTED` 无完成事件的判断，以及 ContextBuilder 对未闭合 Turn 的投影。

### REV-20260820-001-F06

```yaml
finding_id: REV-20260820-001-F06
disposition: accepted
rationale: >
  M1 必须直接解析最终用户数据目录，否则 M2 会产生二次迁移。workspace identity 的规范化、
  哈希输入、碰撞验证和移动语义需要在 M1 前冻结。
resolution_revision: pending
```

处置：在 CLI 架构中写入规范化与 SHA-256 identity 合同；M1 建立最终 DataPaths。

### REV-20260820-001-F07

```yaml
finding_id: REV-20260820-001-F07
disposition: accepted
rationale: >
  内部状态、UI 标签和 UserAction 术语不一致会直接扩散到实现、测试和学习出口，修复成本低。
resolution_revision: pending
```

处置：统一 `UserAction` tagged union，并增加内部状态到 UI 标签映射表。

### REV-20260820-001-F08

```yaml
finding_id: REV-20260820-001-F08
disposition: accepted
rationale: >
  现有命名测试不能单独证明所有 FR，需加强接口行为覆盖；但不要求每个命令各建一个测试函数，
  可用参数化命令表加关键状态变更行为测试避免测试结构复制实现结构。
resolution_revision: pending
```

处置：补充命令表、缺 Key 提示/退出码、idle Ctrl+C、恢复原文件保留和恢复 Session 出口测试。

### REV-20260820-001-F09

```yaml
finding_id: REV-20260820-001-F09
disposition: accepted
rationale: >
  M1 继续写 workspace/.jdagent/sessions、M2 再迁移到用户数据目录会产生不必要的二次迁移。
resolution_revision: pending
```

处置：M1 直接采用最终 DataPaths；M2 只增加 Catalog 和 legacy 非破坏性导入。

### REV-20260820-001-F10

```yaml
finding_id: REV-20260820-001-F10
disposition: deferred
rationale: >
  Windows 键位、控制台关闭事件和 Prompt Toolkit/asyncio 集成必须在真实终端实现前冻结，
  但不阻塞 M1-M5 的应用接口与 Scripted Terminal。
resolution_revision: null
```

触发条件：M6/M7 开工前批准 Windows 平台附录，固定目录、备用换行键、信号能力和人工矩阵。

### REV-20260820-001-F11

```yaml
finding_id: REV-20260820-001-F11
disposition: accepted
rationale: >
  Python 工程规范仍包含初始化前文字，版本来源、TOML 技术选择和依赖加入时机也需闭环。
resolution_revision: pending
```

处置：配置使用标准库 `tomllib`；版本读取 `importlib.metadata`；M1 进入 `0.2.0.dev0`，M9
最终验收升为 `0.2.0`；Prompt Toolkit/Rich 到 M6 才加入运行依赖；清理过时规范。

### REV-20260820-001-F12

```yaml
finding_id: REV-20260820-001-F12
disposition: accepted
rationale: >
  当前状态和 reviews 目录文字应消除误读。强杀测试需要严格隔离，但不在生产路径增加由环境
  变量开启的测试专用分支；优先使用 Fake、可注入 Writer、测试 Tool 和子进程 barrier Adapter。
resolution_revision: pending
```

处置：区分 v0.1/v0.2 状态，更新 AGENTS 说明，并修改 M7 的测试故障注入约束。

## 修订后的开工闸门

```text
M0b  评审处置与开工合同冻结
  ├─ Runtime Recovery 补充
  ├─ Headless/退出码/JSON/flag 合同
  ├─ Workspace identity 与最终 DataPaths
  ├─ ApprovalOutcome 与 Session Rule 生命周期
  ├─ 状态/UserAction 词汇
  └─ 版本、tomllib、依赖与文档清理

M2 前  Session 生命周期事件与 Schema 兼容记录
M3 前  异常恢复 Session 持久表示记录
M4 前  Session Rule 事件 payload 与 Permission 接口细化
M6/M7 前  Windows 平台附录
```

延后项未满足对应闸门时，不得开始该里程碑。它们不阻塞更早且不依赖该决策的里程碑。

## 剩余风险

- 本轮没有运行产品代码、真实终端或 DeepSeek；处置只证明文档合同一致。
- Session 生命周期事件、异常恢复 Session 表示和 Windows 平台细节仍按上述闸门延后。
- `resolution_revision` 需在本轮文档修订提交后回填；未经用户授权，本轮不 commit。
