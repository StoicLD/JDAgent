# REV-20260830-001：v0.3 P1 RAG 实现独立评审处置

状态：`Implemented`

评审方：Cursor（独立评审任务，只读）

处置方：实现者；本记录不是第二次独立评审，也不批准 M0 合同

评审日期：2026-08-30

处置日期：2026-08-30

被评审 revision：`40b9aee520e3a980a1b69e2e1e694eb219d6b7e7`

处置 revision：`2b282284e075a52d63b43f49e262737a988d3299`

原始结论：`fail`（0 Blocker，High 未关闭）

## 范围与证据

独立评审覆盖 `7bd67c5`..`40b9aee` 的 M3–M6 P1 实现，对照已批准架构、ADR-0006、P1 附页和实施计划。M0 需求/合同/Eval 规格保持 `Proposed`；评审与处置都不把它们视为已批准。

原始报告保存在宿主会话，不是产品仓库依赖。本文件保留 finding、处置和验证边界。

## Cursor finding 处置

| Finding | 处置 | 严重度 | 处置依据与结果 | `resolution_revision` |
| --- | --- | --- | --- | --- |
| `REV-20260830-001-F01` | accepted | high | Catalog 打开失败或 Binding 后 `KnowledgeError` 记为 `unavailable`；缺失 Catalog 文件仍为 `not_configured`。 | `2b282284e075a52d63b43f49e262737a988d3299` |
| `REV-20260830-001-F02` | accepted | high | 查询向量化按 Embedding Profile 分组捕获失败，只使该组 `QUERY_FAILED`。 | 同上 |
| `REV-20260830-001-F03` | accepted / rejected | high | 接受：生产 Retrieve 默认不挂 File/Hash；`RuntimeOptions.allow_local_index` 仅测试/Live Fake 选用。拒绝：CLI ingest 在无 endpoint 时仍用本地 File Index（离线 M2 与无 `pymilvus` 基础安装）。 | 同上 |
| `REV-20260830-001-F04` | accepted | high | Collection 使用 `mixed_zh_en_v1`（Jieba + lowercase）与 BM25 Function；Function 检索失败才回退本地打分；hits 携带 `knowledge_base_id`。 | 同上 |
| `REV-20260830-001-F05` | accepted | high | 失败保留 `saga_stage`；INDEXING 可 resume 并激活已写入 revision；过期租约仅同 `operation_id` 或 `takeover=True`；reactivate 复制新 chunk，不改写历史 `valid_to`。 | 同上 |
| `REV-20260830-001-F06` | accepted / rejected | high | 接受：生命周期/墓碑用 `citation_target` 与 ingest 夹具覆盖 deactivate openable / delete tombstone。拒绝：Hash Fake 上强制 hybrid drop ≤ 0.02；Gold Q090–Q092 仍跳过（模板查询，由夹具覆盖）。 | 同上 |
| `REV-20260830-001-F07` | accepted | medium | `OpenAICompatibleEmbedding` 在提供 `api_key` 时发送 `Authorization: Bearer`。Coordinator 仍未按 `credential_ref` 解析密钥。 | 同上 |
| `REV-20260830-001-F08` | accepted | medium | 删除知识库时 sources 与 bindings 均映射 `STILL_REFERENCED`。 | 同上 |
| `REV-20260830-001-F09` | deferred | medium | 长 I/O 心跳与旧 generation GC 仍待 P1.8/P1.9 专项。触发：Milvus Live 或长 ingest 卡住。 | — |
| `REV-20260830-001-F10` | accepted | medium | Headless `citations[]` 增加 `source_version_id`、`snapshot_id`、`content_hash`、`locator_schema_version`。 | 同上 |
| `REV-20260830-001-F11` | deferred | medium | `prepare`/ingest 仍有同步 SQLite/Milvus I/O。触发：事件循环卡顿证据。 | — |
| `REV-20260830-001-F12` | rejected | medium | 无 Citation 时 precision=1.0 是常规 IR；completeness 已为 0。 | — |
| `REV-20260830-001-F13` | deferred | medium | 含 tool-call 的中间 assistant 文本仍不跑 Citation 校验。触发：模型在工具轮次写入非法引用。 | — |
| `REV-20260830-001-F14` | accepted | low | `JsonlSession` docstring 改为 schema-v2。 | 同上 |
| `REV-20260830-001-F15` | accepted | low | 失败路径 PTK 使用查询 SHA-256 fingerprint。 | 同上 |
| `REV-20260830-001-F16` | deferred | low | Repair 流不记 usage、不通知 observer。 | — |
| `REV-20260830-001-F17` | deferred | low | Parent 超预算仍 `continue` 而非只做 tail-drop。 | — |

### 原始证据与影响

以下 location 均指被评审 revision `40b9aee`。

| Finding | Location | Claim | Impact |
| --- | --- | --- | --- |
| F01 | `coordinator._prepare`；`empty_prepared_knowledge` | Catalog 损坏或 Binding 后失败记为 `not_configured`。 | Headless/TRR 把坏知识说成未配置。 |
| F02 | `preparation.py` 向量化循环 | 一组 embed 失败变成整次 prepare 失败。 | 违反 P1.3 / 架构 §11.1 分组隔离。 |
| F03 | `coordinator`、CLI ingest、默认 EmbeddingProfile | 生产 Retrieve 走 Hash+SQLite。 | 与架构 §5.3 / ADR-0006 默认真实索引冲突。 |
| F04 | `milvus.search_bm25` | 全表 query + 本地正则；无 Analyzer Function。 | 不是批准的 BM25 路径；hits 缺 `knowledge_base_id`。 |
| F05 | `ingestion`、`acquire_lease` | 失败回写 RECEIVED；reconcile 只过期租约；新 `operation_id` 可抢过期租约；reactivate 把 `valid_to` 改回 0。 | Saga 不收敛，历史 Revision Filter 被改写。 |
| F06 | `test_v03_eval`、`SKIP_QUERIES`、`citation.py` | hybrid 门禁 `drop <= 1.0`；Q090–Q092 跳过；无墓碑 resolver。 | §17.4 生命周期/tombstone 未测。 |
| F07 | `embedding.py` | HTTP embeddings 无 `Authorization`。 | 真实 OpenAI-compatible 端点无法认证。 |
| F08 | `catalog.delete_knowledge_base` | 仍被 source 引用时可能 `IntegrityError`。 | 不是稳定 `STILL_REFERENCED`。 |
| F10 | `headless.py` | 完成事件级 Citation 字段被丢掉。 | Headless 与事件事实不一致。 |

## 未关闭项

- M0 需求、合同、Eval 规格、学习清单仍为 `Proposed`。
- Milvus Standalone Live（`JDAGENT_RUN_RAG_LIVE=1`）未跑；BM25 Function 是否被目标 server 接受只能在 Live 证实。
- Hybrid drop ≤ 0.02 仍只属于真实 embedding Live，不在 Hash Fake 上声称通过。
- Coordinator 未把 `credential_ref` 接到 Embedding Adapter。
- F09/F11/F13/F16/F17 按上表延期。
- 本处置不是对 `2b28228` 的独立复核；若需要关闭“独立实现评审”门禁，须由未参与该修复的评审方对新 SHA 取证。

## 验证

**已运行：** `uv run ruff check src tests`、`uv run ruff format --check src tests`、`uv run pyright`、`uv run pytest -q` → `168 passed, 5 skipped`；`git diff --check` 无冲突。

**未运行：** DeepSeek Live、Gold Answer/Citation Live、Milvus Live、clean-env wheel 重装。原因：默认离线门禁；本机无 Milvus；修复不改依赖图。
